diff --git a/hdl/projects/pluto/system_bd.tcl b/hdl/projects/pluto/system_bd.tcl
index 8eeff72ab..e90f8cb6e 100755
--- a/hdl/projects/pluto/system_bd.tcl
+++ b/hdl/projects/pluto/system_bd.tcl
@@ -17,9 +17,17 @@ create_bd_port -dir I spi0_sdo_i
 create_bd_port -dir O spi0_sdo_o
 create_bd_port -dir I spi0_sdi_i
 
-create_bd_port -dir I -from 17 -to 0 gpio_i
-create_bd_port -dir O -from 17 -to 0 gpio_o
-create_bd_port -dir O -from 17 -to 0 gpio_t
+# 18 EMIO GPIO in the stock design; 22 here. The extra four (bits 21:18) are
+# the header pins that tx_gpio_bitmap hands back to Linux whenever the
+# bit-map flag is clear, so they stay usable as ordinary GPIO.
+create_bd_port -dir I -from 21 -to 0 gpio_i
+create_bd_port -dir O -from 21 -to 0 gpio_o
+create_bd_port -dir O -from 21 -to 0 gpio_t
+
+# The four header pins themselves, driven either by EMIO GPIO or by the low
+# nibble of the transmit sample. See tx_gpio_bitmap.v.
+create_bd_port -dir O -from 3 -to 0 sample_gpio_o
+create_bd_port -dir O -from 3 -to 0 sample_gpio_t
 
 create_bd_port -dir O spi_csn_o
 create_bd_port -dir I spi_csn_i
@@ -53,7 +61,7 @@ ad_ip_parameter sys_ps7 CONFIG.PCW_FPGA0_PERIPHERAL_FREQMHZ 100.0
 ad_ip_parameter sys_ps7 CONFIG.PCW_FPGA1_PERIPHERAL_FREQMHZ 200.0
 
 ad_ip_parameter sys_ps7 CONFIG.PCW_GPIO_EMIO_GPIO_ENABLE 1
-ad_ip_parameter sys_ps7 CONFIG.PCW_GPIO_EMIO_GPIO_IO 18
+ad_ip_parameter sys_ps7 CONFIG.PCW_GPIO_EMIO_GPIO_IO 22
 ad_ip_parameter sys_ps7 CONFIG.PCW_SPI1_PERIPHERAL_ENABLE 0
 ad_ip_parameter sys_ps7 CONFIG.PCW_I2C0_PERIPHERAL_ENABLE 0
 ad_ip_parameter sys_ps7 CONFIG.PCW_SD0_PERIPHERAL_ENABLE 1
@@ -221,6 +229,49 @@ ad_add_interpolation_filter "tx_fir_interpolator" 8 2 1 {61.44} {7.68} \
 ad_ip_instance xlslice interp_slice
 ad_ip_instance util_upack2 tx_upack
 
+# TX sample nibble -> header GPIO.
+#
+# The AD9361's DAC is 12 bits and takes dma_data[15:4]; dma_data[3:0] are
+# discarded inside the FPGA. tx_gpio_bitmap puts those four otherwise-wasted
+# bits on four header pins, in lockstep with the sample that carried them, so
+# they can act as a master clock / frame clock / sync line for external
+# receivers. Enabled by bit 1 of the DAC core's GP_CONTROL register
+# (up_dac_gpio_out, AXI offset 0xBC); bit 0 is already the interpolator
+# bypass above.
+#
+# add_files here rather than via adi_project_files in system_project.tcl:
+# this file is sourced from inside adi_project_create, which runs first, so
+# the module is not in the fileset yet.
+add_files -norecurse $ad_hdl_dir/projects/pluto/tx_gpio_bitmap.v
+create_bd_cell -type module -reference tx_gpio_bitmap tx_bitmap
+
+# bit 1 of up_dac_gpio_out = the bit-map enable flag
+ad_ip_instance xlslice bitmap_sel [list \
+  DIN_WIDTH 32 DIN_FROM 1 DIN_TO 1 DOUT_WIDTH 1]
+
+# the low nibble of the RAW DMA sample, before the interpolator - a FIR mixes
+# neighbouring samples together, so the nibble downstream of it would be
+# filter output rather than the pattern the user authored
+ad_ip_instance xlslice nibble_slice [list \
+  DIN_WIDTH 16 DIN_FROM 3 DIN_TO 0 DOUT_WIDTH 4]
+
+# bits 21:18 of the EMIO GPIO, which own the pins when the flag is clear
+ad_ip_instance xlslice gpio_bitmap_o [list \
+  DIN_WIDTH 22 DIN_FROM 21 DIN_TO 18 DOUT_WIDTH 4]
+ad_ip_instance xlslice gpio_bitmap_t [list \
+  DIN_WIDTH 22 DIN_FROM 21 DIN_TO 18 DOUT_WIDTH 4]
+
+# "fifo_rd_data just changed", which is what the nibble has to be captured on.
+# util_upack2 registers fifo_rd_data and then raises exactly one of
+# fifo_rd_valid / fifo_rd_underflow on the cycle the new word is standing at
+# its output - valid for a real sample, underflow for the zeros it substitutes
+# when the DMA has starved. OR-ing them means the pins carry the low nibble of
+# whatever the DAC is being given, including those zeros, with no exceptions to
+# write down.
+ad_ip_instance util_vector_logic bitmap_valid_or [list \
+  C_OPERATION {or} \
+  C_SIZE 1]
+
 ad_ip_instance axi_dmac axi_ad9361_adc_dma
 ad_ip_parameter axi_ad9361_adc_dma CONFIG.DMA_TYPE_SRC 2
 ad_ip_parameter axi_ad9361_adc_dma CONFIG.DMA_TYPE_DEST 0
@@ -323,6 +374,36 @@ ad_connect  tx_upack/fifo_rd_underflow axi_ad9361/dac_dunf
 ad_connect axi_ad9361/up_dac_gpio_out interp_slice/Din
 ad_connect  tx_fir_interpolator/active interp_slice/Dout
 
+# TX sample nibble -> header GPIO. fifo_rd_data_0 is channel-0 I straight out
+# of the unpacker; fifo_rd_data_1 is Q, the hook for a future I+Q widening.
+#
+# The strobe is NOT fifo_rd_en (logic_or/Res). fifo_rd_en is the REQUEST for a
+# sample; util_upack2 registers fifo_rd_data, so the requested word only
+# reaches the output on the following clock. Capturing on fifo_rd_en would
+# latch the previous sample, one behind the DAC forever. fifo_rd_valid /
+# fifo_rd_underflow are the registered pair that go high with the data, which
+# is why they are OR-ed together above.
+#
+# Capturing on a per-sample strobe at all, rather than on l_clk, is the other
+# half of this: in 2R2T mode a sample arrives only every second l_clk, and
+# capturing on the clock would silently double the rate of every authored
+# pattern.
+ad_connect axi_ad9361/l_clk tx_bitmap/clk
+ad_connect axi_ad9361/rst tx_bitmap/rst
+ad_connect tx_upack/fifo_rd_data_0 nibble_slice/Din
+ad_connect nibble_slice/Dout tx_bitmap/sample_in
+ad_connect tx_upack/fifo_rd_valid bitmap_valid_or/Op1
+ad_connect tx_upack/fifo_rd_underflow bitmap_valid_or/Op2
+ad_connect bitmap_valid_or/Res tx_bitmap/valid_in
+ad_connect axi_ad9361/up_dac_gpio_out bitmap_sel/Din
+ad_connect bitmap_sel/Dout tx_bitmap/flag
+ad_connect sys_ps7/GPIO_O gpio_bitmap_o/Din
+ad_connect sys_ps7/GPIO_T gpio_bitmap_t/Din
+ad_connect gpio_bitmap_o/Dout tx_bitmap/gpio_o_in
+ad_connect gpio_bitmap_t/Dout tx_bitmap/gpio_t_in
+ad_connect tx_bitmap/pin_o sample_gpio_o
+ad_connect tx_bitmap/pin_t sample_gpio_t
+
 ad_connect  axi_ad9361/l_clk axi_ad9361_adc_dma/fifo_wr_clk
 ad_connect  axi_ad9361/l_clk axi_ad9361_dac_dma/m_axis_aclk
 ad_connect  cpack/fifo_wr_overflow axi_ad9361/adc_dovf
diff --git a/hdl/projects/pluto/system_constr.xdc b/hdl/projects/pluto/system_constr.xdc
index ff020ba04..e762a81a0 100755
--- a/hdl/projects/pluto/system_constr.xdc
+++ b/hdl/projects/pluto/system_constr.xdc
@@ -50,6 +50,64 @@ set_property  -dict {PACKAGE_PIN  J20 IOSTANDARD LVCMOS25} [get_ports gpio_ctl[3
 set_property  -dict {PACKAGE_PIN  P20  IOSTANDARD LVCMOS25} [get_ports gpio_en_agc]
 set_property  -dict {PACKAGE_PIN  R19  IOSTANDARD LVCMOS25} [get_ports gpio_resetb]
 
+# Header GPIO, broken out on connector JP5 as 3V3_IO1..4 (pins 7, 9, 11, 13).
+# Free in the stock design; driven here either by Linux (EMIO GPIO) or by the
+# low nibble of the transmit sample.
+#
+# Balls read off the vendor schematic, sheet 5 (U1G, "PL端BANK13"):
+#   3V3_IO1 = V10 (IO_L20P)   3V3_IO3 = U10 (IO_L12N)
+#   3V3_IO2 = U9  (IO_L16P)   3V3_IO4 = T9  (IO_L12P)
+# sample_gpio[n] is wired to 3V3_IO(n+1), so the bit number matches the silk.
+#
+# LVCMOS33 is right: sheet 1 ties VCCO_13_1..4 (T8, U11, W7, Y10) to VCC3V3.
+# Note this is NOT the same as the rest of the design, which declares LVCMOS25
+# and LVDS_25 on banks 34/35 that the same sheet supplies from VCC1V8 - an
+# inconsistency inherited from ADI's stock Pluto constraints, left alone here.
+#
+# Do not guess these. V11, W9 and V7 are adjacent bank-13 balls and look like
+# plausible candidates, but the schematic marks them "no connect" - and a wrong
+# PACKAGE_PIN is not a build error, it is a bitstream that drives a pad wired
+# to nothing.
+#
+# PULLDOWN gives the pins a DEFINED idle state. Each of these four nets appears
+# exactly twice in the whole schematic - once at the FPGA ball, once at JP5 -
+# so there is no external pull, series part or ESD diode anywhere on them, and
+# nothing for an internal pull to fight. Undriven they would genuinely float,
+# which for a line something downstream reads as a trigger is the wrong
+# default: a floating input that drifts high looks asserted. Idle low is the
+# de-asserted state for a sync or frame signal, so pull down rather than up.
+#
+# These four outputs are deliberately left UNCONSTRAINED, like every other
+# LVCMOS output in this design. Skew between them is then whatever the router
+# gives - a few hundred picoseconds, set by placement, not checked by timing
+# analysis. That is far below one sample period (16 ns at 61.44 MSPS) and so
+# irrelevant for the frame and sync signals this feature exists for. If you
+# are doing something where it is not irrelevant, add a set_output_delay
+# against the AD9361 datapath clock and re-run implementation; do not assume
+# the numbers here have been verified, because they have not been.
+set_property  -dict {PACKAGE_PIN  V10  IOSTANDARD LVCMOS33 PULLTYPE PULLDOWN} [get_ports sample_gpio[0]]
+set_property  -dict {PACKAGE_PIN  U9   IOSTANDARD LVCMOS33 PULLTYPE PULLDOWN} [get_ports sample_gpio[1]]
+set_property  -dict {PACKAGE_PIN  U10  IOSTANDARD LVCMOS33 PULLTYPE PULLDOWN} [get_ports sample_gpio[2]]
+set_property  -dict {PACKAGE_PIN  T9   IOSTANDARD LVCMOS33 PULLTYPE PULLDOWN} [get_ports sample_gpio[3]]
+
+# The bit-map enable flag is written by software in the AXI clock domain and
+# read in the AD9361 datapath domain, so it is a clock-domain crossing. That
+# is exactly what the two flip-flops inside tx_gpio_bitmap are for. Timing it
+# as an ordinary synchronous path is not conservative, it is meaningless - and
+# measured, it made this the worst path in the whole design (+0.186 ns, where
+# the stock design has +0.214 ns) for no reason. Bound the crossing instead:
+# -datapath_only keeps the two synchroniser flops close enough together for
+# metastability to settle, without pretending the launch edge means anything.
+#
+# Written as a single command: an .xdc is a restricted Tcl dialect and "if" is
+# NOT one of the commands it accepts - Vivado answers a guarded version with
+# "Command 'if' is not supported in the xdc constraint file" and silently drops
+# the whole block. (It says so in pluto.runs/*/runme.log, not in the top-level
+# build log.) No guard is needed anyway: these lines only exist in the file
+# once the bit-map patch is applied, and then the cell always exists.
+set_max_delay -datapath_only 4.000 \
+  -to [get_pins -quiet -hier -filter {NAME =~ *tx_bitmap*flag_m_reg/D}]
+
 set_property  -dict {PACKAGE_PIN  T15  IOSTANDARD LVCMOS25} [get_ports enable]
 set_property  -dict {PACKAGE_PIN  P18  IOSTANDARD LVCMOS25} [get_ports txnrx]
 
diff --git a/hdl/projects/pluto/system_top.v b/hdl/projects/pluto/system_top.v
index 5e4718878..29a903476 100755
--- a/hdl/projects/pluto/system_top.v
+++ b/hdl/projects/pluto/system_top.v
@@ -84,6 +84,10 @@ module system_top (
   inout   [ 3:0]  gpio_ctl,
   inout   [ 7:0]  gpio_status,
 
+  // header pins: EMIO GPIO, or the low nibble of the TX sample when the
+  // bit-map flag (up_dac_gpio_out[1]) is set. See tx_gpio_bitmap.v.
+  inout   [ 3:0]  sample_gpio,
+
   output          spi_csn,
   output          spi_clk,
   output          spi_mosi,
@@ -96,9 +100,12 @@ module system_top (
 
   // internal signals
 
-  wire    [17:0]  gpio_i;
-  wire    [17:0]  gpio_o;
-  wire    [17:0]  gpio_t;
+  wire    [21:0]  gpio_i;
+  wire    [21:0]  gpio_o;
+  wire    [21:0]  gpio_t;
+
+  wire    [ 3:0]  sample_gpio_o;
+  wire    [ 3:0]  sample_gpio_t;
 
   // instantiations
 
@@ -115,6 +122,16 @@ module system_top (
 
   assign gpio_i[16:14] = gpio_o[16:14];
 
+  // The bit-map pins. gpio_i[21:18] carries what is actually on them back to
+  // Linux, so reading the GPIO still works while the fabric owns them.
+  ad_iobuf #(
+    .DATA_WIDTH(4)
+  ) i_iobuf_sample_gpio (
+    .dio_t (sample_gpio_t),
+    .dio_i (sample_gpio_o),
+    .dio_o (gpio_i[21:18]),
+    .dio_p (sample_gpio));
+
 
 
   system_wrapper i_system_wrapper (
@@ -143,6 +160,8 @@ module system_top (
     .gpio_i (gpio_i),
     .gpio_o (gpio_o),
     .gpio_t (gpio_t),
+    .sample_gpio_o (sample_gpio_o),
+    .sample_gpio_t (sample_gpio_t),
     .iic_main_scl_io (iic_scl),
     .iic_main_sda_io (iic_sda),
     .rx_clk_in_p (rx_clk_in_p),
diff --git a/hdl/projects/pluto/tx_gpio_bitmap.v b/hdl/projects/pluto/tx_gpio_bitmap.v
new file mode 100644
index 000000000..47ff1ecad
--- /dev/null
+++ b/hdl/projects/pluto/tx_gpio_bitmap.v
@@ -0,0 +1,140 @@
+// ***************************************************************************
+// ***************************************************************************
+// Copyright (C) 2026 fishball7020-fpga-devkit contributors
+//
+// Distributed under the GNU General Public License version 2, which can be
+// found in the top level directory of this repository (LICENSE), and also
+// online at: <https://www.gnu.org/licenses/old-licenses/gpl-2.0.html>
+//
+// This module is distributed in the hope that it will be useful, but WITHOUT
+// ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or
+// FITNESS FOR A PARTICULAR PURPOSE.
+// ***************************************************************************
+// ***************************************************************************
+
+// tx_gpio_bitmap - drive header pins from the low bits of the transmit sample.
+//
+// WHY THIS EXISTS
+// ---------------
+// The AD9361's transmit DAC is 12 bits wide, but the sample words you write
+// over DMA are 16 bits. The converter takes the top 12 (`dma_data[15:4]`) and
+// the bottom 4 (`dma_data[3:0]`) are thrown away inside the FPGA. They cost
+// nothing and reach nothing.
+//
+// This module takes those four discarded bits and puts them on four header
+// pins instead. Because they ride along with the sample that produced them,
+// each pin transition is locked to the RF that left the antenna port on that
+// same sample clock - no drift, no unknown delay. That is what makes them
+// usable as a master clock, a frame clock or an external sync line for a
+// separate receiver in a multi-channel radar or MIMO setup.
+//
+// The FPGA does not *generate* anything here; it transports. Whatever pattern
+// you author into the low nibble of the samples you transmit is what appears
+// on the pins. Alternate bit 0 between 0 and 1 every sample and that pin is a
+// square wave at half the sample rate; set bit 1 once every 16 samples and
+// that pin is a frame marker. See docs/tx-gpio-bitmap.md.
+//
+// CONTROL
+// -------
+// `flag` selects what owns the pins:
+//
+//   flag = 0   the pins are ordinary EMIO GPIO, driven by Linux (`gpio_*_in`)
+//   flag = 1   the fabric claims them and drives the registered sample bits
+//
+// `flag` comes from bit 1 of the DAC core's GP_CONTROL register
+// (`up_dac_gpio_out`, AXI offset 0xBC); bit 0 is already the interpolator
+// bypass. It is asynchronous to `clk`, so it is synchronised here.
+//
+// GAPPED VALID MATTERS
+// --------------------
+// `sample_in` is only meaningful on clocks where `valid_in` is high - in
+// 2R2T mode the datapath presents a new sample every second `l_clk`. The
+// nibble is captured on those clocks only and held in between, so a pattern
+// authored per SAMPLE comes out per sample. Capturing every clock would
+// double the apparent rate and is the mistake the testbench exists to catch.
+//
+// `valid_in` must mean "a new word is standing at `sample_in` NOW", not "a
+// word has been requested". In this design it is
+// `fifo_rd_valid | fifo_rd_underflow` from util_upack2, which is exactly
+// that; `fifo_rd_en` is one clock too early and would hand the pins the
+// previous sample forever. See system_bd.tcl.
+//
+// LATENCY
+// -------
+// The nibble reaches the pin one clock after the sample leaves the unpacker.
+// The matching RF leaves the antenna port much later - through the fabric
+// interpolator, the AD9361's own digital filters and the DAC. So the pins
+// LEAD the RF by a fixed offset. Fixed is what matters: it does not drift and
+// it repeats run to run for a given configuration, so it can be calibrated
+// out once. It is not zero, and nothing here pretends otherwise.
+
+`timescale 1ns/100ps
+
+module tx_gpio_bitmap #(
+
+  parameter integer NBITS = 4
+) (
+
+  input                    clk,
+  input                    rst,
+
+  // datapath: the raw DMA sample and its valid strobe.
+  //
+  // X_INTERFACE_IGNORE keeps Vivado's IP packager from deciding these ports
+  // are a bus. Left alone it sees a vector next to a port whose name ends in
+  // _valid, infers a data/valid signal interface, and turns the port into an
+  // interface pin - at which point ad_connect refuses to wire a plain slice
+  // output to it ("Cannot connect non-interface to interface"). The names
+  // below also follow ad_fs4_ddc's valid_in/*_in convention, which packages
+  // as plain pins. Belt and braces: this costs one build to discover.
+
+  (* X_INTERFACE_IGNORE = "true" *) input   [NBITS-1:0]  sample_in,
+  (* X_INTERFACE_IGNORE = "true" *) input                valid_in,
+
+  // control: bit 1 of up_dac_gpio_out
+
+  (* X_INTERFACE_IGNORE = "true" *) input                flag,
+
+  // standard-GPIO source, used when flag = 0
+
+  (* X_INTERFACE_IGNORE = "true" *) input   [NBITS-1:0]  gpio_o_in,
+  (* X_INTERFACE_IGNORE = "true" *) input   [NBITS-1:0]  gpio_t_in,
+
+  // to the pins (via an ad_iobuf at the top level)
+
+  (* X_INTERFACE_IGNORE = "true" *) output  [NBITS-1:0]  pin_o,
+  (* X_INTERFACE_IGNORE = "true" *) output  [NBITS-1:0]  pin_t
+);
+
+  reg     [NBITS-1:0]      sample_d = {NBITS{1'b0}};
+  reg                      flag_m   = 1'b0;
+  reg                      flag_s   = 1'b0;
+
+  // Capture the nibble once per sample, not once per clock.
+
+  always @(posedge clk) begin
+    if (rst == 1'b1) begin
+      sample_d <= {NBITS{1'b0}};
+    end else if (valid_in == 1'b1) begin
+      sample_d <= sample_in;
+    end
+  end
+
+  // Two flip-flops across the clock-domain boundary. The flag is set once by
+  // software and then left alone, so all this has to do is keep a metastable
+  // level out of the fabric.
+
+  always @(posedge clk) begin
+    if (rst == 1'b1) begin
+      flag_m <= 1'b0;
+      flag_s <= 1'b0;
+    end else begin
+      flag_m <= flag;
+      flag_s <= flag_m;
+    end
+  end
+
+  assign pin_o = (flag_s == 1'b1) ? sample_d : gpio_o_in;
+  assign pin_t = (flag_s == 1'b1) ? {NBITS{1'b0}} : gpio_t_in;
+
+endmodule
